Atue como um Engenheiro de Frontend Sênior e Especialista em UI/UX de E-commerce de Luxo.

refatore @app/product/[id]/page.tsx

A versão Mobile está perfeita e DEVE ser mantida EXATAMENTE como está (imagem no topo com 50vh, barra de compra fixa no rodapé). No entanto, a versão Desktop/Tablet precisa ser totalmente reimaginada usando a técnica de Aprimoramento Progressivo (prefixos `md:` e `lg:` do Tailwind).

Siga ESTRITAMENTE estas diretrizes de Arquitetura e UI/UX:

1. Atualização de Dados (`lib/data.ts`):
   - Adicione uma propriedade opcional `flavors?: string[]` à interface/tipo dos produtos.
   - Atualize os `MOCK_PRODUCTS` para incluir sabores em alguns produtos (ex: Brigadeiro Gourmet com ["Ao Leite", "Meio Amargo", "Pistache"]).

2. Layout Desktop (Split Screen - Duas Colunas):
   - No desktop (`md:`), o container principal deve ser centralizado e contido (ex: `md:max-w-6xl md:mx-auto md:pt-16 md:px-8`).
   - Mude a estrutura principal para um Grid de duas colunas: `md:grid md:grid-cols-2 md:gap-12 lg:gap-16`.

3. Coluna da Esquerda (Imagem - Desktop):
   - A imagem que no mobile ocupa `h-[50vh]` em tela cheia, no desktop deve assumir uma proporção quadrada (`md:aspect-square md:h-auto`) com bordas arredondadas elegantes (`md:rounded-3xl`) e overflow hidden.

4. Coluna da Direita (Conteúdo e Interação - Desktop):
   - Título, descrição e preço devem se alinhar perfeitamente nesta coluna.
   - Adicione a nova UI de "Escolha de Sabores" (caso o produto tenha a propriedade `flavors`). Faça um seletor visual em formato de "pills" (botões arredondados) com hover state e estado de "selecionado" em dourado (`brand-gold`).
   - Mantenha a lista de "Opções Adicionais".

5. O Desafio do Botão de Compra (Crucial):
   - A barra fixa de "Adicionar" (`fixed bottom-0`) é exclusiva do Mobile. Adicione a classe `md:hidden` nela para que ela desapareça no desktop.
   - Crie uma SEGUNDA instância desses mesmos botões (Quantidade + Adicionar) que apareça APENAS no desktop (`hidden md:flex`), posicionada organicamente no final da coluna da direita, abaixo das opções. No desktop, os botões não precisam ocupar 100% da largura, podem ter um tamanho mais contido.

6. Regras Estruturais e Idioma:
   - O componente DEVE ser uma Arrow Function (`export const ProductPage = () => { ... }`).
   - O código DEVE conter `"use client";` na primeira linha.
   - Todo o código, variáveis, funções e imports ESTRITAMENTE em INGLÊS.
   - Todo o texto da interface (frontend) ESTRITAMENTE em Português do Brasil (pt-BR).
   - NENHUM comentário é permitido dentro do código final.
   - Microinterações no desktop devem ser aplicadas (ex: `md:hover:-translate-y-1`, `transition-all duration-300`).
