Atue como um Engenheiro de Software Especialista em Next.js (App Router) e React. Fornecerei um código React bruto exportado do Figma. Sua tarefa é refatorar, componentizar e adaptar este código para se encaixar perfeitamente na minha arquitetura Next.js.

voce deve sempre seguir essas regras .claude/rules

Execute os seguintes passos para o código fornecido:

1. Limpeza de Código e Arquitetura (Next.js App Router):
   - Remova qualquer sintaxe do `react-router-dom` (ex: `<Outlet />`, `Link to="..."`).
   - Substitua todas as tags `<a>` e propriedades `to="..."` pelo componente `<Link href="...">` do Next.js.
   - Substitua todas as tags `<img>` nativas pelo componente `<Image />` do Next.js.
   - Substitua caminhos de imagem genéricos do Figma (`figma:asset/...`) por placeholders temporários (ex: `/images/placeholder.png`) e adicione as propriedades `width` e `height` adequadamente.
   - Separe a interface em componentes lógicos (ex: se eu enviar uma página inteira, extraia o Header, Footer e Hero para seus próprios arquivos em uma pasta `/components`).

2. Interatividade e Estado:
   - O Next.js usa Server Components por padrão. Mantenha os arquivos como Server Components, a menos que utilizem hooks (`useState`, `useEffect`) ou eventos de clique (`onClick`).
   - Se um componente exigir estado (como abrir um menu mobile), isole essa parte específica em um Client Component e adicione a diretiva `"use client";` no topo do arquivo.
   - Corrija qualquer hook `useEffect` quebrado (ex: garanta que arrays de dependência `[]` estejam presentes para evitar loops infinitos).

3. Estilização e Ícones:
   - Garanta que todas as classes do Tailwind CSS sejam preservadas e otimizadas.
   - Para ícones ausentes (como `Menu`, `X`, `ShoppingBag`), importe-os da biblioteca `lucide-react`.

4. REGRAS ESTRITAS A SEGUIR:
   - Variáveis, nomes de funções, lógica, nomes de pastas e componentes DEVEM ser escritos estritamente em INGLÊS.
   - Todo o texto visível ao usuário, elementos de interface e tipografia DEVEM estar em Português do Brasil (pt-BR).
   - O código final NÃO DEVE conter nenhum comentário. Escreva um código limpo e autoexplicativo. Não explique o código dentro dos arquivos.
   - Foco absoluto na estética de interface "Mobile-first". O layout mobile deve estar impecável antes de ser escalado para telas maiores.
